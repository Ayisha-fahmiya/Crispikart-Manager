import 'package:flutter/material.dart';

class OrdersDeliveredScreen extends StatelessWidget {
  const OrdersDeliveredScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Orders Delivered"),
      ),
    );
  }
}
